/** Machine-readable role slugs, matching the backend's `role.slug` values. */
export enum RoleEnum {
    ADMIN = 'admin',
    ORG_ADMIN = 'org_admin',
    BANK_ADMIN = 'bank_admin',
    VOLUNTEER = 'volunteer',
    USER = 'user',
    PERSONAL_WORKSPACE = 'personal_workspace',
}
