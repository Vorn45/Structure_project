export enum ProjectStatus {
    DRAFT = 'draft',
    IN_PROGRESS = 'in_progress',
    STOP = 'stop',
    COMPLETED = 'completed',
    EXPIRED = 'expired',
}

export enum ProjectStatusEnum {
    DRAFT = 1,
    IN_PROGRESS = 2,
    STOP = 3,
    COMPLETED = 4,
    EXPIRED = 5,
}
