export enum CustomFieldType {
    TEXT = 'text',
    TEXTAREA = 'textarea',
    NUMBER = 'number',
    BOOLEAN = 'boolean',
    DATE = 'date',
    DATETIME = 'datetime',
    SELECT = 'select',
    MULTI_SELECT = 'multi_select',
    USER = 'user',
    URL = 'url',
    EMAIL = 'email',
    PHONE = 'phone',
}
